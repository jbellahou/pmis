@{
    WebSecurity.RequireAuthenticatedUser();
    if (!Context.User.IsInRole("edit_organization_" + Request.QueryString["id"]))
    {
        Response.Redirect("~/account/unauthorized");
    }
}